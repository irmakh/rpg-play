# Persistent Memory

> This file contains curated long-term facts, preferences, and context that persist across sessions.
> The AI reads this at the start of each session. You can edit this file directly.

## User Preferences

- Project name: char_sheet — D&D virtual tabletop / character sheet app
- Application code lives in `Application/` folder
- Working directory: `f:\proj\char_sheet`
- Git remote: github.com/irmakh/rpg-play, branch: main
- Commit author email: irmakh@gmail.com (already set in repo config — verify before committing)
- Deploy via Docker Compose; HTTPS on port 443 with Let's Encrypt certs

## App Feature Inventory

> Major screens and features — know what exists before building new things.

- **Character sheet** (`index.js`) — HP tracking, inventory, spells, portrait, conditions display
- **DM Table / Map** (`table.js`) — token placement/drag, drawing tool, initiative tracker (click-to-view, auto-advance, previous turn), HP panel, conditions panel, token labels always visible
- **DM Panel** (`dm.js`) — monster management, loot, map prep (clone + positional placement), media gallery, shop
- **Events screen** — DM-only campaign event log/tracker
- **Monster names** — hidden from players; players see identifier only (e.g. "Goblin #1")
- **Token conditions** — D&D 5e status conditions (poisoned, stunned, etc.) shown on tokens + HP tracker with 5e.tools links; any player can toggle conditions on their own token
- **Token operation queue** — all token add/move/remove network requests are serialised through an operation queue to prevent race conditions under concurrent player interaction
- **Drawing tool** — real-time freehand drawing on the table map, synced to all clients via SSE/WebSocket
- **Shop** — merchant items with tag system for filtering
- **Backup/restore** — per-section (characters, monsters, media, loot, shop, etc.), non-destructive import (merges, does not wipe existing data)
- **Image system** — uploads generate original + `_thumb.webp` (80×80) + `_medium.webp` (max 500px); maps always full quality
- **Move tool** — 500ms delay before drag activates (prevents accidental moves)
- - **Calendar (Calendar of Harptos)** — DM calendar at events.html (DM-only, password-gated); player calendar tab in index.html. Tenday (3×10) grid display. Festival days: Midwinter, Greengrass, Midsummer, Shieldmeet (leap years), Highharvestide, The Feast of the Moon. DM sets current campaign date (day-by-day or jump). Events have title, description, FR date, public/DM-only flag, event type. SSE broadcasts calendar-updated on changes.
- table.html/table.css/table.js: left sidebar layout (240px #left-panel) — Initiative, Chat, Token/HP sections. No floating panels. Both sidebars have toolbar toggle buttons.
- table.js has been split into 12 modules under Application/public/js/table/ (commit e4bc4b9). The monolithic file no longer exists. table.html loads the 12 files in order via defer script tags.
- OpenAI (ChatGPT) added as third AI DM provider (session 54). Key stored as openaiApiKey (separate from openrouterApiKey). Provider tab added in setup, settings modal, change-model modal. Curated model list: gpt-4.1-mini (default), gpt-4.1, gpt-4.1-nano, gpt-4o, gpt-4o-mini, o4-mini, o3-mini.
- Initiative system fully rewritten (session 60, FRONTEND_VERSION 85). Endpoints: GET /api/initiative; POST /api/initiative/entries (upsert by charId — players+monsters); PATCH /api/initiative/entries/:id (DM name/roll); PATCH /api/initiative/entries/:id/roll (roll only); DELETE /api/initiative/entries/:id; POST start/next/prev/end/clear/cleanup. Upsert uses ldb.getInitEntryByCharId (precise SQL, charId != '' so never matches monster/empty entries). CRITICAL: roll paths must send the TARGET character's charId, never the active-turn char — rollInitiativeFromPanel derives charId from live _sideQrollTokenId panel token (NOT _sideCharId which lags an async fetch, NOT getActiveTurnTokenId). _startCharInitRoll always POSTs charId. Player roll modal = #init-roll-modal (replaced window.prompt). SSE actions: updated/start/next/prev/end/clear. Old paths (/api/initiative/roll, /add, PUT /:id, PATCH /:id/roll) all removed.
- Index-page initiative tracker (public/js/index/index-initiative.js, renderInitiativeTracker) masks monster entries for non-DM viewers: shows only the identifier (last word of e.name via initMonsterIdentifier helper) and hides edit/remove buttons on monster rows. DM (indexIsDM()) sees full names + buttons. Mirrors tokDisplayName fallback on the table. FRONTEND_VERSION 87.
- Character stat real-time sync (AC/HP/speed) between index sheet and table tokens is fully bidirectional over SSE (session 62, FRONTEND_VERSION 88). Server: PUT /api/characters/:id syncs hpCurrent/hpMax/hpTemp/speed/ac to linked tokens + broadcasts token-updated; PUT /api/table/tokens/:id writes HP back to character hpcur/hpmax/hptemp + broadcasts characters updated. AC total lives in character data.ac. Frontend: table-hppanel.js _refreshHpPanel reads live tok.ac; table-realtime.js token-updated re-renders initiative, characters-updated re-fetches _fetchInitCharData when char is in initiative. Index autosaves on edit (debounced) and applies SSE via applyData+recalcAll.
- Chat sender (table screen, v91): getChatSender() returns sessionCharName for player sessions (players always post as their OWN character, never the selected token) and qrollCharName (selected token side-panel name) for the DM, fallback 'DM'. sessionCharName global loaded from session in table-auth.js _loadSession.
- Table movement/combat (v89-91): token movement open to everyone (no ownership/monster/turn gates, client+server); single-level Undo button #btn-undo-move snaps last move back (lastTokenMove global). Persistent dice-mode toggle #rp-dice-mode (Normal/Adv/Dis under name) replaces per-roll adv-modal — qroll() calls confirmRoll(diceMode) directly. Weapon row click rolls attack then shows #weapon-dmg-prompt (Miss/Roll Damage). Spell name click posts description (spell array s[6]) to chat as raw HTML via new chat html:true flag (chat-render renders unescaped). DM image send reveals #image-reveal-modal on all clients (reveal:true on /api/chat/image broadcasts table 'image-reveal') instead of inline chat. Drag activation delay 100ms. Roll button removed from left initiative header. /api/initiative/prev no longer requires masterAuth (symmetric with /next).
- Actions tab (FRONTEND_VERSION 94, session 64): character sheet '⚔️ Actions' tab (public/js/index/index-actions.js) aggregates weapon attacks (hidden #wpn-tbl fed by Inventory weapons), action-flagged spells, and custom actions. Custom actions = global 'actions' array stored as _actions JSON (+ _actionIdCounter) on character data; shape {id,name,category(action|bonus|reaction|other),description,dice,uses,used,recharge(short|long|'')}. Spell array gained TWO appended indices: [13]=action category (.spell-action dropdown) and [14]=duration (sortable 'Dur' column after Range). Spell notes/duration read via .spell-notes/.spell-duration CLASSES (not positional) due to the new column shifting input order; sortSpells/_spellCellVal use Conc/Ritual/School at cols 6/7/8. Table right panel renders Actions/Bonus/Reactions/Other with clickable limited-use boxes synced via PATCH /api/characters/:id/action-use + POST /api/characters/:id/action-rest (DM or char auth, broadcast 'characters updated'); qroll exposes _actions. Clicking any spell on the table posts it to chat (even empty desc) with a 5e.tools link appended. Bulk Edit panel has visible/hidden buttons (bulkSetVisibility, DM-only). DM monster right-panel header shows identifier-first 'G7 [Goblin]'.
- Table screen v95 (session 65): Dice animation on/off switch in toolbar next to 🎲 (#btn-dice-anim) — window.diceAnimEnabled (localStorage 'diceAnimEnabled', default on); dice-engine.js showDiceAnimation early-returns when false. Dice overlay #dice-overlay bg is transparent (no dim). DM shared-media reveal modal is a draggable card (#image-reveal-card, var(--bg2) bg, ✕ top-right, NO outside-click close, drag is LOCAL-only never broadcast). Table click-offset fix: getCanvasPos measures from canvasWrap.getBoundingClientRect() not #canvas-area (the inline-block wrapper added a ~10px line-box gap).
- Table right panel reveal gotcha: the details container #rp-token-details (which holds #side-qroll-content) is hidden behind #rp-placeholder by default. openHpPanel(tok) is what REVEALS it (sets details.display='', hides placeholder, adds 'rp-open' to #side-panel). selectToken(id) only highlights the token + calls loadSideQroll() (writes content) — it does NOT reveal the panel. To show a combatant in the right panel programmatically, call selectToken(id) THEN openHpPanel(tok) (this is exactly what a map token click does via _selectTokenClick). This is why initiative-row click (table-initiative.js viewInitEntry, session 70) must call both. (FRONTEND_VERSION now 107)
- Calendar journals + media (session 71, FRONTEND_VERSION 108): calendar_events gained author_char_id/author_name/media_json. Visibility rule: viewer sees event iff isDM OR is_public=1 OR author_char_id=viewerCharId. ldb.listCalendarEvents now takes {isDM,charId} (was publicOnly bool); ldb.getCalendarEvent(id) added. Player journals authored from index calendar tab (+Journal button/modal, default SHARED); shared=>public, private=>DM+author; eventType forced 'journal'. POST/PUT/DELETE /api/calendar/events accept DM (master pw) OR character (X-Character-Id+X-Character-Password); PUT/DELETE ownership-gated (403 non-author). POST /api/calendar/media = one attachment/call, validates SHARED_MEDIA_MIME (incl audio), 25MB cap, images->processImageSizes('calendar'), av->saveUploadFile, stored as media:[{type,url,thumb}]. DM attaches media on events.html. Tests: tests/api/calendar.api.test.js + calendar support in tests/helpers/make-ldb.js & test-app.js (570 tests total).
- Item bonuses (session 73, FRONTEND_VERSION 115): items carry a generic bonuses array [{target,value}] (target = save-*/skill-*/check-* plus *-all) alongside the legacy acBonus/initBonus/speedBonus/spellAtkBonus/spellDcBonus fields. Shared helpers aggregateItemBonuses(items)+ITEM_BONUS_GROUPS+itemBonusTargetLabel() live in js/lib/dnd-data.js (loaded by index AND table). index-calc.js recalcAll bakes save/skill/PP bonuses into the displayed value fields and check bonuses into the ability mod circle; server recalcDerived (server/routes/characters.js, PATCH /equip) mirrors this so wear/unwear from the table updates saves/skills/pp live; table-panel applies ability-check bonuses live from _items. XML round-trips bonuses. Item modal has a +Add Bonus repeatable row UI (index-items.js).
- Table right panel (session 73, FRONTEND_VERSION 115): classic HUD RETIRED — modern is the only theme (table-main.js initTheme forces data-theme=modern, theme toggle removed). renderSideCharacter shows name->Abilities always-visible, then 4 tabs Skills(Saves+Skills)/Combat/Spells/Items (state _sideActiveTab+setSideTab, default skills, reset on token switch). Combat tab uses SUB-TABS Attacks/Actions/Bonus/Reaction/Other (_sideCombatTab+setSideCombatTab, _sidePanelActionSections returns per-category sections + rest control). Monsters still use renderMonsterFullStats (collapsible, unchanged). Popout (.po-sheet-window in table-sheet-popout.css) = single centered column, tabs sticky on top.
- Asset cache-busting gotcha (session 74, FRONTEND_VERSION 117): the server ?v=N injection middleware (server.js) now resolves DIRECTORY requests to their index.html before the .html check (rel.endsWith('/') => rel+='index.html'), so the character sheet at / (root) gets versioned assets too — previously only paths ending in .html were injected, so / was served raw by express.static with bare immutable-cached JS/CSS that never busted (table.html was fine because it's only reached at /table.html). Separately, pop-out windows (table-popout.js) are built client-side via document.write and NEVER pass through that middleware, so their CSS <link> hrefs must be versioned manually: new _assetVer() reads the ?v= the server injected onto the opener's own assets and appends it to /css/table.css, table-theme-modern.css, table-sheet-popout.css. Rule: anything that emits asset URLs OUTSIDE server-rendered .html (document.write popouts, dynamically-built pages, directory indexes) needs explicit ?v= or it caches immutable forever.

## Key Facts — App Stack

- **Backend:** Node.js ES modules, Express 4, better-sqlite3 (local) or InstantDB (cloud)
- **Frontend:** Vanilla JS — `index.js` (char sheet), `table.js` (DM map), `dm.js` (DM panel), `monsters.js`
- **Real-time:** SSE + WebSockets for broadcast
- **DB mode:** toggled via `DB_PROVIDER` env var — `localdb` (SQLite default) or `instantdb`
- **Key files:** `Application/server.js` (~2600 lines), `Application/db/localdb.js` (~630 lines)
- **Image storage:** files on disk under `public/uploads/{characters,monsters,media,maps}/`; paths stored in SQLite
- Service worker (public/sw.js, rpg-v94+): NETWORK-FIRST for HTML/navigations (request.mode==navigate || destination==document || .html), cache fallback only when offline; CACHE-FIRST for versioned static JS/CSS/img (busted by ?v=N). This was changed from all-cache-first in session 64 because cache-first on HTML served a stale index.html app shell on mobile Chrome PWAs, hiding newly added pages/scripts (the Actions tab failed to load). When frontend won't update on mobile: bump FRONTEND_VERSION + sw CACHE together, redeploy, then on device clear site data / reinstall PWA once to drop the old SW.

## Key Facts — Docker

- Uses `node:20-alpine` with anonymous volume for `/app/node_modules`
- Adding a **native npm package** (e.g. sharp, better-sqlite3) requires full rebuild:
  1. `docker-compose build --no-cache`
  2. `docker-compose down -v && docker-compose up -d`
  — skipping step 2 leaves the stale volume in place and the package stays missing

## Completed Work (2026-04-21) — commit ba9c4a8

- **6 performance fixes:** DB indexes, backup media scan fix, chat cleanup query, initiative cleanup JOIN, map HTTP caching, monster backup parallelization
- **Multi-size image system:** `sharp` added; every image upload now generates `_thumb.webp` (80×80 crop) and `_medium.webp` (max 500px). Maps excluded. DB migrations added to `char_media`, `table_tokens`, `shared_media`. Backup strips derived sizes; restore regenerates them.
- **Frontend wired:** tokens use `portraitThumb`, gallery uses `mediumUrl`, portrait header uses `mediumUrl`, lightbox uses original. Backward-compatible — all fall back to `portrait`/`dataUrl` if thumb absent.
- **`GET /api/characters/:id/portrait`** now returns `{ portrait, portraitThumb }`
- **`POST /api/table/tokens`** now accepts and stores `portraitThumb`

## Learned Behaviors

- Always check `tools/manifest.md` before creating new scripts
- Always check `goals/manifest.md` before starting a task — create a goal if none exists (ask permission first)
- **Session start order (MANDATORY):** run `python tools/memory/memory_read.py --format markdown` — do NOT use `cat` on log files and do NOT start from the auto-memory system (~/.claude/...). Then check `goals/manifest.md` and `tools/manifest.md`.
- **Session close (MANDATORY):** (1) `memory_write.py --content "..." --type event --importance 6`, (2) `memory_write.py --update-memory ...` if new persistent facts, (3) `memory_write.py --sync YYYY-MM-DD`, (4) `embed_memory.py --all`, (5) `git add ... data/memory.db && commit + push`. Always include `data/memory.db` in the commit — it holds the searchable SQLite entries and is tracked by git. Never edit log files or MEMORY.md directly — direct edits miss the SQLite DB layer.
- **hybrid_search.py** — run BEFORE starting work when: (1) task touches a feature worked on in a session older than yesterday; (2) user references a prior decision you don't have in context; (3) you're choosing an approach and want to verify no conflicting past decision exists; (4) debugging and want to check if the same issue was seen before. Use `python tools/memory/hybrid_search.py --query "..."`.
- **memory_db.py** — use `--action search --query "..."` when you know the exact term (function name, config key, library name). Faster and more precise than hybrid search for pinpoint recall.
- **semantic_search.py** — use instead of hybrid when: (1) finding entries similar to a specific known entry by ID (`--similar-to <id>`, unique to this tool); (2) hybrid returns no useful results and you want a pure meaning-based pass; (3) keyword terms are polluting hybrid results. Use `python tools/memory/semantic_search.py --query "..."` or `--similar-to <id>`.
- **embed_memory.py** — run `--all` at session close (step 4) and any time search returns no results on entries you know exist. Uses local BAAI/bge-small-en-v1.5 model via fastembed — no API key required.
- Update `tools/manifest.md` immediately when a new tool/helper is created
- Never modify or create goals without explicit user permission
- Follow GOTCHA framework layers — don't collapse orchestration, tools, and goals into one place
- Read full goals before starting tasks — don't skim
- initiative init field stores dex+items only (NOT init-bonus). init-bonus is always added on top in all roll paths (table.js, index.js, server.js token placement). /api/characters/:id/qroll now returns 'ac' field (added to EXTRA_KEYS).
- server.js refactored into 12 route modules under Application/server/routes/. Each exports register(app, ctx). server.js is a lean entry point (~364 lines). OpenRouter image generation uses /api/v1/chat/completions + modalities:['image'] + reads from message.images[0].image_url.url.
- pscp uploads run directly via Bash tool when user asks — do not just print commands. Server-side commands (pm2, docker) are still printed for user to run manually.
- Table panel pop-out (table-popout.js, FRONTEND_VERSION 97/98): togglePopout('left-panel'|'side-panel'|'chat-bar') moves the REAL DOM node into a same-origin window (window.open blank + adoptNode into #po-host). Opener's document.getElementById/querySelector/querySelectorAll are patched to also search open popout docs (no-op when none open) so all render/SSE code transparently updates the moved panel; opener global functions copied onto popout window (window-control methods denylisted) so inline on* handlers resolve. Docks back on the popup's pagehide/beforeunload (node still alive), NOT just the 600ms close-poll, then dispatches 'popout-docked' {detail:{id}} which table-panel.js listens for to force _sideQrollTokenId=null+loadSideQroll() rebind (else SSE/API for the right panel stay dead until reselect). Chat scrolls to newest on pop-out open AND dock. Toolbar buttons btn-pop-left/right/chat. Right panel has an Items wear/unwear section (_buildSidePanelItems, 2-per-row checkboxes before spell slots) backed by PATCH /api/characters/:id/equip which recomputes AC/init/speed/sp-dc via recalcDerived() (mirrors index-calc.js recalcAll) + token sync; _items added to qroll payload. Character XML export/import (index-char.js) now round-trips custom actions + spell action($[13])/duration($[14]); XML import asks update-vs-new-copy on same-name match.
- escJs() helper (js/lib/esc.js + each standalone page/inline-HTML that defines its own esc) escapes a value for a single-quoted JS string inside an HTML attribute: onclick="fn('${escJs(v)}')". Use it (NOT esc/escAttr) for ANY interpolation inside an inline event-handler single-quote arg — esc() doesn't escape ' and HTML-entity escaping fails there (browser decodes &#39; to a bare quote before inline JS parses), so apostrophes (e.g. "Drizzt's Sword") break the handler. esc() stays correct for HTML text + double-quoted attributes. (session 70, commit 4c5feb8)
- Table right-panel & bulk-edit control convention (session 72, FRONTEND_VERSION 113): section sub-headers use class .rp-flat-hdr (gold bar — color var(--ac), background var(--a22), border-top), the SAME class the character-sheet sections (Abilities/Skills/Attacks/Items) use; do NOT use plain gray .rp-block-lbl text. Controls sit in a padded .rp-blk-body below. GOTCHA styling number inputs on the table screen: table.css has global input[type=number]{width:100%} (spec 0,1,1) that beats a plain .class (0,1,0) — write input[type=number].rp-num-input (0,2,1) or the input stretches full-width (inline style=width also wins, why old inputs worked). Dice-mode toggle (#rp-dice-mode/setDiceMode) moved into top #toolbar and now defaults to 'ask'.

## Current Projects

- Character Sheet Application — active development; base features complete, ongoing optimization and feature work

- Stories system: manual comic upload workflow. storiesdb.js (separate SQLite DB). Three screens: stories.html (dashboard), story-builder.html (panel editor with character cast multiselect by portrait), story-viewer.html (grid/strip + lightbox + cast strip). Password gate via POST /api/auth/verify-any accepts DM or any character password.
---

*Last updated: 2026-06-19*
*This file is the source of truth for persistent facts. Edit directly to update.*
